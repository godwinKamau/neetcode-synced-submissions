class Solution {
    /**
     * @param {string} s
     * @param {string} t
     * @return {boolean}
     */
    isAnagram(s, t) {

        let counter = new Array(26).fill(0)

        "a".charCodeAt()

    }
}
